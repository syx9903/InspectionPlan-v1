services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: detection-plan-postgres

    environment:
      POSTGRES_DB: detection_plan
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

    restart: unless-stopped

volumes:
  postgres_data: